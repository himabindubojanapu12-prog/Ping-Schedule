# Interview Scheduling Agent — Python Dependencies

# Core LLM
anthropic>=0.40.0

# Google Calendar API
google-auth>=2.25.0
google-auth-oauthlib>=1.2.0
google-auth-httplib2>=0.2.0
google-api-python-client>=2.111.0

# Email (standard library — no install needed)
# smtplib, imaplib, email — built into Python 3.x

# Utilities
python-dateutil>=2.8.2
pytz>=2024.1
